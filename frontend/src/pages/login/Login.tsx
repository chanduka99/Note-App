import React from 'react'

interface Props { }

function Login(props: Props) {
    const { } = props

    return (
        <div className='flex justify-center items-center'>
            <p className='bg-blu'>Login</p>
        </div>
    )
}

export default Login;
